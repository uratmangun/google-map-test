# Add this PublishPort line to ~/.config/containers/systemd/termux-stack.pod
# (with the other PublishPort=127.0.0.1:300x:300x entries):
#
#   PublishPort=127.0.0.1:3004:3004
#
# Cloudflare ingress (config.remote.yml), before http_status:404:
#
#   - hostname: maps.uratmangun.ovh
#     service: http://termux-stack:3004
